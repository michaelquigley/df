# 2026-07-10

- Terminus review `e5e092fe6cd3`: vetoed `dd-strict-deep-embedded-pointer`. Recursive allocation would change pre-existing forgiving-mode behavior; strict mode already fails closed by rejecting the unrecognized field. Treat any deep anonymous-pointer allocation change as separate compatibility work.
